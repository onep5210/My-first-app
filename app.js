// CMD (cd, dir, mkdir, etc.) & Ctrl + C = clear

// node package for create web server ---> Express
// NPM (node package manager)
// nmp init <-- create package.JSON
// npm install express <-- install the express
// npm install -g nodemon <--- don;t need to re-run when change the server if installed > nodemon index.js **don't user if doing a lot of things at once

// console.log(); // global

// setTimeout();
// clearTimeout();

// setInterval();
// clearInterval();

// console.log (module)
console.log(__filename);
console.log(__dirname);

const log = require("./logger")     // ../ = root folder


log("Hello")

// npm install dotenv <--- for hiding api keys with environment variables

// Pass to Github
// git init
// git add .
// git commit

// Github >> + >> New repository

// Paste Gitbug command into cmd